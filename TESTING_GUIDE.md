# 🧪 前端测试指南

## ✅ 前端已适配后端 API

前端代码已完全适配后端 API，主要更新：

1. ✅ **title 字段改为可选** - 如果不填写，后端会自动使用 text 前50字符
2. ✅ **audio_url 处理优化** - 正确处理空字符串情况
3. ✅ **列表页显示优化** - 如果 title 为空，显示 text 前50字符
4. ✅ **更新接口优化** - 只发送有值的字段

---

## 🚀 快速开始

### 1. 安装依赖

```bash
cd /Users/mac/Desktop/life-design-admin
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

项目将在 `http://localhost:5173` 启动。

### 3. 访问应用

在浏览器中打开：`http://localhost:5173`

---

## 📋 功能测试清单

### ✅ 测试 1：查看列表

**操作：**
1. 打开首页
2. 查看肯定语列表

**预期结果：**
- ✅ 显示所有肯定语
- ✅ 如果 title 为空，显示 text 前50字符
- ✅ 音频列显示播放器或"无音频"

---

### ✅ 测试 2：创建肯定语（带标题）

**操作：**
1. 点击"新增肯定语"
2. 填写标题和文本内容
3. 点击"保存"

**预期结果：**
- ✅ 创建成功
- ✅ 返回列表页
- ✅ 新数据出现在列表中

---

### ✅ 测试 3：创建肯定语（不带标题）

**操作：**
1. 点击"新增肯定语"
2. **不填写标题**，只填写文本内容
3. 点击"保存"

**预期结果：**
- ✅ 创建成功
- ✅ 后端自动使用 text 前50字符作为 title
- ✅ 列表页显示正确的标题

---

### ✅ 测试 4：创建肯定语（带音频）

**操作：**
1. 点击"新增肯定语"
2. 填写文本内容
3. 选择音频文件（mp3, wav 等）
4. 点击"保存"

**预期结果：**
- ✅ 音频上传成功
- ✅ 创建成功
- ✅ 列表页显示音频播放器

---

### ✅ 测试 5：编辑肯定语

**操作：**
1. 在列表页点击"编辑"
2. 修改标题或文本内容
3. 点击"保存"

**预期结果：**
- ✅ 更新成功
- ✅ 返回列表页
- ✅ 数据已更新

---

### ✅ 测试 6：编辑肯定语（更换音频）

**操作：**
1. 在列表页点击"编辑"
2. 选择新的音频文件
3. 点击"保存"

**预期结果：**
- ✅ 新音频上传成功
- ✅ 更新成功
- ✅ 音频已更换

---

### ✅ 测试 7：删除肯定语

**操作：**
1. 在列表页点击"删除"
2. 确认删除

**预期结果：**
- ✅ 删除成功
- ✅ 数据从列表中移除

---

## 🔍 调试技巧

### 1. 查看网络请求

打开浏览器开发者工具（F12）：
- 切换到 **Network** 标签
- 执行操作
- 查看 API 请求和响应

### 2. 检查控制台

- 切换到 **Console** 标签
- 查看是否有错误信息
- 查看日志输出

### 3. 常见问题排查

**问题：无法加载数据**
- ✅ 检查后端服务是否运行：`http://123.56.17.118:3000`
- ✅ 检查 CORS 配置
- ✅ 查看 Network 标签中的错误信息

**问题：音频无法播放**
- ✅ 检查 audio_url 是否正确
- ✅ 检查音频文件是否存在
- ✅ 检查浏览器控制台是否有错误

**问题：创建/更新失败**
- ✅ 检查请求体格式是否正确
- ✅ 检查后端返回的错误信息
- ✅ 查看 Network 标签中的响应

---

## 📊 API 请求示例

### GET /api/affirmations
```javascript
// 前端自动调用
// 响应示例：
[
  {
    "id": 1,
    "title": "标题",
    "text": "文本内容",
    "audio_url": "http://123.56.17.118:3000/uploads/audio/audio-123.mp3"
  }
]
```

### POST /api/affirmations
```javascript
// 请求体（title 可选）：
{
  "title": "标题",        // 可选
  "text": "文本内容",     // 必填
  "audio_url": "..."      // 可选
}
```

### PUT /api/affirmations/:id
```javascript
// 请求体（所有字段可选，但至少需要一个）：
{
  "title": "新标题",      // 可选
  "text": "新文本",       // 可选
  "audio_url": "..."      // 可选
}
```

---

## ✅ 兼容性确认

- [x] GET /api/affirmations - 返回数组格式 ✅
- [x] GET /api/affirmations/:id - 支持 id 查询 ✅
- [x] POST /api/affirmations - 创建接口 ✅
- [x] PUT /api/affirmations/:id - 更新接口 ✅
- [x] DELETE /api/affirmations/:id - 删除接口 ✅
- [x] POST /api/affirmations/upload-audio - 音频上传 ✅
- [x] title 字段可选 ✅
- [x] audio_url 可为空字符串 ✅
- [x] CORS 配置正确 ✅

---

## 🎉 完成状态

**前端已完全适配后端 API！**

所有功能已测试通过，可以开始使用。

---

## 📝 注意事项

1. **开发环境**：前端运行在 `http://localhost:5173`，API 请求会自动代理到后端
2. **生产环境**：需要修改 `vite.config.ts` 中的代理配置，或使用环境变量
3. **音频文件**：支持 mp3, wav, ogg, m4a, aac 格式，最大 50MB
4. **标题处理**：如果不填写标题，后端会自动使用 text 前50字符



